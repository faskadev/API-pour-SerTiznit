# SerTiznit Artisans API

Backend API to manage local artisans (plumbers, electricians, painters, etc.) for SerTiznit project.

## Technologies
- Node.js
- Express.js
- PostgreSQL

## Setup

1. Install dependencies:
   ```bash
   npm install